# Reusable Code Area
For generalized reusable code/adapters/templates only. Remove business-specific data and secrets; define inputs/outputs, dependencies, supported versions, security assumptions and tests; record provenance in ../REGISTRY.md and maturity status. Do not place raw project-specific code here merely because it works.
